platform :ios

output_path 'GasAll/Resources/Localizables'

source :google_drive, 
       :spreadsheet => '[Localizables] GasAll', 
       :login => ENV['GOOGLE_LOGIN'], 
       :password => ENV['GOOGLE_PASSWORD']

# If the app has [i] tags or something like that, uncomment this
# except :keys => '[\[][a][\]]'
